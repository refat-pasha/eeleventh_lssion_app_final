// placeholder
// lib/app/theme/colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF4E6EFF);
  static const Color secondary = Color(0xFF22C55E);

  // Backgrounds
  static const Color backgroundDark = Color(0xFF0B0F1A);
  static const Color backgroundLight = Color(0xFFF5F7FB);

  // Surfaces / Cards
  static const Color cardDark = Color(0xFF141A2A);
  static const Color cardLight = Color(0xFFFFFFFF);

  // Text
  static const Color textPrimaryDark = Color(0xFFFFFFFF);
  static const Color textSecondaryDark = Color(0xFFB8C1D1);

  static const Color textPrimaryLight = Color(0xFF111827);
  static const Color textSecondaryLight = Color(0xFF6B7280);

  // Status Colors
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);

  // Divider / Border
  static const Color border = Color(0xFF2A3145);

  // Transparent
  static const Color transparent = Colors.transparent;
}